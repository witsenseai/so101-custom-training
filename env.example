# env.example — Copy to env.local and fill in your secrets
# This file shows what needs to be configured
# DO NOT commit env.local to git (add it to .gitignore)

# Hugging Face credentials
export HF_TOKEN=hf_your_token_here

# Optional: Override HF user if different from default
# export HF_USER=your_hf_username
